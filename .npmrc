# Prevent npm workspaces from hoisting dependencies to root node_modules/.
# Without this, npm 11+ moves packages like picocolors, source-map-js out of
# app/node_modules/ into root/, breaking Turbopack's module resolution.
# This only affects developers building from source.
install-strategy=nested
