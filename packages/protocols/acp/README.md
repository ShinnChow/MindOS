# @mindos/acp

MindOS Agent Client Protocol core package.

This package owns ACP registry metadata, local agent detection, subprocess
lifecycle, and ACP session management. Web-specific adapters such as A2A
bridging and MindOS knowledge-base tools live in `packages/web/lib/acp`.
